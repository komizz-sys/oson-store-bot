aiogram==3.20.0
aiosqlite==0.20.0
python-dotenv==1.0.1
httpx==0.27.2
fastapi==0.115.0
uvicorn==0.30.6
aiohttp==3.10.10
# Подпись TON-транзакций для автооплаты покупок (см. services/ton_wallet.py).
# Если по какой-то причине не поставится — бот всё равно запустится, просто
# автоплатёж выключится и покупки снова пойдут через ручную ton://-ссылку.
tonutils>=0.3.0
